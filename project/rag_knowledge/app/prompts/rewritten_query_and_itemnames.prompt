当前用户问题: {query}

请根据历史会话和当前问题, 提取用户正在询问的主体名称 -> item_names.
1. 如果用户明确提到了主体名称, 请提取出来. 可能有一个或多个, 但不能重复;
2. 如果用户使用了代词(如"这个", "它"), 请结合历史会话指代消解, 确定主体名称;
3. 如果无法确定主体名称, item_names 返回空列表;
4. 请重新改写用户的问题 -> rewritten_query, 使其成为包含主体名称的独立完整问题, 注意不要超过 100 个字符.
5. 用户的提问可能会切换主体, 和历史会话不一定一致, 因此要以当前用户问题为主进行识别和问题改写

请直接返回 JSON 格式结果, 格式如下:
{{
    "item_names": ["主体A", "主体B"],
    "rewritten_query": "关于主体A和主体B, ..."
}}

历史会话:
{history_text}
