# handcraft_agent

从零手写的轻量 AI Agent 运行时框架 + 智能客服 demo。目标：吃透 AI Agent 底层原理（agent loop、状态管理、流式、安全、成本、可观测、测试、评估），覆盖企业级 Agent 项目的常见问题、难点与面试考点。

## 项目背景

主流 Agent 框架（LangChain / LangGraph / DeepAgents / Claude Agent SDK）把 agent loop 封装成黑盒——开发者只配 tools + prompt，框架内部「模型 ↔ 工具」的循环、状态管理、流式推送都被藏起来了。

本项目反其道而行，**手写 agent loop**，把每一层暴露出来：

- 模型如何被调用、工具如何被执行、结果如何回填
- 状态如何持久化、断点如何续跑
- 流式事件如何产生、如何推给前端
- 重试、熔断、超时、幂等如何兜底
- prompt injection、沙箱、HITL、脱敏、审计如何落地

**与 LangGraph 的关系**：LangGraph 用 StateGraph 显式建模「状态机 + 节点」，本项目用最朴素的 while 循环实现同一件事。理解了手写版，再看 LangGraph / DeepAgents，就知道它们替你做了什么、为什么那么设计。

## 核心架构

agent loop 是项目核心，本质是一个 while 循环：

```text
while not done:
    response = model.generate(messages, tools)       # 模型决策：给答案 or 调工具
    if response.has_tool_calls:                      # 模型要求调用工具
        results = execute_tools(response.tool_calls) # 并发执行所有工具
        messages.append(results)                     # 结果回填，进入下一轮
    else:                                            # 模型给出最终答案
        done = True
```

其中「模型决策 → 工具执行 → 结果回填」的一次完整迭代称为一个 **Turn**。回填不是把工具结果拼进文本，而是以一条 `tool_result` 消息的形式追加进 `messages`——模型下一轮通过完整消息历史理解执行情况。

在这个循环之上，逐层叠加生产级能力：

| 层 | 能力 | 对应难点 / 选型 |
|----|------|----------------|
| 模型层 | ChatModel 协议 + httpx/openai 双实现 | 选型 0001 / 0003 |
| 工具层 | @tool 装饰器 + JSON schema 生成 + 沙箱 + 工具设计 | #10, #24, #70 |
| 循环层 | 并行工具 + 错误自纠错 + 循环防护 | #1-3 |
| 状态层 | checkpoint 快照 + thread 分区 + 数据模型 | #5, #12, 选型 0002 |
| 流式层 | 四类事件 + SSE | #4, 选型 0005 |
| 可靠层 | 重试 / 熔断 / 超时 / 幂等 / 降级 | #13-19 |
| 队列层 | 并发排队 + 长任务异步化 | #20, 选型 0006 |
| 安全层 | 注入防护 / 沙箱 / HITL / 脱敏 / 审计 / 输出护栏 | #23-30 |
| 记忆层 | 四层记忆 + 多租户隔离 + 记忆机制 | #31-33 |
| 成本层 | 成本追踪 / token 计量 / 分级路由 / 缓存 / batch | #34-37 |
| 可观测层 | 日志 / 指标 / 告警 / 版本化 / trace | #38-41 |
| 评估层 | Agent 评估 / 调试 / 数据飞轮 | #58-60 |
| 测试层 | mock / 确定性 / 分层 / 轨迹断言 / 快照 | #61-63 |
| 工程层 | 部署扩展 / 异步并发 / 延迟优化 / 工程化底座 | #64-67 |

> #68/#69/#70 横切点贯穿所有层：工具描述优化与工具设计（tool 层）、温度/采样控制与 prompt 工程（model 层）、限流/背压（server 层）。

## 技术栈

| 类别 | 技术 |
|------|------|
| 语言 | Python 3.13 |
| 模型接入 | httpx 裸调 + openai SDK（DeepSeek 兼容 base_url） |
| 存储 | Redis（缓存 / checkpoint / 分布式锁）+ Postgres（checkpoint 历史） |
| 向量库 | Chroma（起步）+ pgvector / Milvus（生产选型） |
| 文档解析 | pypdf / unstructured |
| 流式 | SSE（Server-Sent Events） |
| 队列 | 进程内 asyncio 队列（P1），预留分布式 MQ |
| 测试 | pytest + pytest-asyncio + respx（mock HTTP） |
| 日志 | structlog + Loki |
| 可观测 | OpenTelemetry + Langfuse（trace 可视化） |
| 错误上报 | Sentry |
| 前端 demo | Vue 3 + EventSource |

## 技术选型

### 0001 薄 ChatModel 协议（不做多 provider 抽象）

定义最小 `ChatModel` 协议（`generate(messages, tools) -> ModelResponse`），P0 只实现 DeepSeek 一种模型。

- **理由**：LangChain 式胶水层（`init_chat_model` 统一 OpenAI/DeepSeek/Qwen）过度抽象，偏离「手写 loop、理解底层」主线；协议隔离已足以演示「模型与 runtime 解耦」
- **备选**：多 provider 抽象层（拒绝：偏离主线）；裸调不抽象（拒绝：讲不出解耦设计）
- **后果**：接新模型需新增实现类（而非改配置）；协议必须足够薄，否则退化成胶水层

### 0002 Checkpoint 用 Redis + Postgres 双实现（跳过 SQLite）

`CheckpointSaver` 协议提供 InMemory（测试）、Redis、Postgres 三种实现，运行时配置切换。

- **理由**：Redis（KV 快照，快但弱一致）与 Postgres（关系型、有事务、支持历史 / time-travel）代表两种存储语义，双实现能演示「存储介质不同，checkpoint 语义也不同」；SQLite 虽零依赖，但无法体现并发与事务语义差异
- **备选**：SQLite（拒绝：无法体现并发与事务差异）；仅 InMemory（拒绝：无法演示生产级持久化）
- **后果**：需维护 Redis/Postgres 连接管理；本地运行需 Redis + Postgres 服务

### 0003 LLM 调用层用 httpx 裸调 + openai SDK 双适配器

同时提供两种 DeepSeek（OpenAI 兼容）接入：httpx 裸调（自己拼 `/chat/completions`、解析 tool_calls、处理 SSE 流式）和 openai SDK 适配器。

- **理由**：httpx 裸调能看清 OpenAI 兼容协议细节，契合「理解底层」的教学目标；openai SDK 是生产实际做法；两者并列，便于对比「SDK / 框架帮你藏了什么」
- **备选**：仅 openai SDK（拒绝：看不到协议细节）；仅 httpx 裸调（拒绝：不贴近生产实际）
- **后果**：两套 model 实现需保持行为一致（用同一组测试约束）

### 0004 通用框架 + 客服 demo 双形态

核心是业务无关的 agent runtime 库，另用一个智能客服 / 工单 demo 自证可用。

- **理由**：纯框架缺业务落地，面试时「框架能干嘛」说服力弱；纯业务则框架级难点（并行工具、checkpoint 通用性）被业务逻辑掩盖；双形态既覆盖全部框架难点，又能「吃自己的狗粮」
- **备选**：纯框架（拒绝：缺业务落地）；纯业务（拒绝：框架难点被掩盖）
- **后果**：需维护框架层 + demo 层两套代码；demo 设计要覆盖全部框架难点

### 0005 流式输出用 SSE，而非 WebSocket

所有流式输出（agent 进度、工具调用事件、token 流）统一用 SSE。

- **理由**：agent 流式输出是服务器→客户端的单向推送，SSE 的单向性刚好匹配；且 LLM 流式 API（OpenAI/Anthropic）本身返回 SSE 格式，可直通转发。WebSocket 的双向能力在此场景是多余能力，还引入协议升级、心跳、重连、LB（Load Balancer，负载均衡器）粘性成本
- **备选**：WebSocket（拒绝：双向能力多余）；轮询（拒绝：延迟高、浪费请求）
- **后果**：客户端用 EventSource 消费；双向交互（如 HITL 需客户端主动推送）需单独处理

### 0006 异步任务队列用进程内 asyncio 起步，预留分布式 MQ

难点 #20「并发请求排队 + 长任务异步化」需要一个任务队列。P1 用进程内 asyncio 队列（FIFO + 并发状态锁），通过 `TaskQueue` 抽象预留分布式 MQ 切换。

- **理由**：进程内队列零部署、能完整演示排队/打断/拒绝/背压语义；分布式 MQ 引入 broker 运维成本，单实例 demo 阶段是过度设计；抽象层保证未来切分布式 MQ 不改上层
- **备选**：直接上分布式 MQ（拒绝：过度设计）；无抽象裸写 asyncio 队列（拒绝：无法演示「队列可替换」）
- **后果**：进程内队列重启即丢；多实例部署时需替换为分布式 MQ

## 分阶段计划

| 阶段 | 范围 | 难点 | 产出物 | 验收标准 |
|------|------|------|--------|----------|
| **P0** | 核心 loop + 可靠性 + 测试 | #1-5、#10-11、#13、#61-63、#65 | model.py + tool.py + loop.py + stream.py + retry.py + checkpoint/ + tests/ | 带工具的 agent loop 跑通；checkpoint 可断点续跑；mock LLM 单测通过 |
| **P1** | 可靠性加固 + 安全 + RAG + demo | #6-7、#14-27、#29-30、#35、#38、#45-47 | server（SSE 接口）+ 客服 demo + 安全/降级/队列/RAG 模块 | 客服 demo 端到端跑通（提问→检索→回复→转人工）；SSE 流式输出；高危操作需审批 |
| **P2** | 记忆/成本/可观测/多 agent/能力扩展/评估/工程化 | #8-9、#12、#28、#31-34、#36-37、#39-44、#48-60、#64、#66-67 | 记忆/成本/可观测/多 agent/MCP/Skills/Eval/数据模型/部署/工程底座模块 | 完整生产级能力：多租户隔离、成本追踪、指标告警、多 agent 协作、评估回归、无状态水平扩展 |

> #68/#69/#70 横切点横跨 P0-P1：工具描述优化、温度/采样控制、prompt 工程、工具设计方法论属 P0；限流/背压属 P1。

## 难点清单

共 **70 个编号难点**，按 14 类组织：

| ID | 类别 | 难点 | 详细细节 | 阶段 |
|----|------|------|----------|------|
| 1 | ① 核心循环 | 并行工具调用 + 部分失败 | • 模型一次响应可同时发起多个工具调用（`tool_calls` 是一个数组），runtime 要并发执行它们而非串行等待，缩短总耗时 | P0 |
| | | | • 并发执行可能部分成功、部分失败，失败的工具不能拖垮成功的工具，成功结果和失败原因都要一起回填给模型 | |
| | | | • 并行的结构标志是「多个 `tool_calls` 在同一个 assistant message 里」，回填或回放时必须保持这一并行语义 | |
| | | | • 如果把并行结果拆成多个顺序轮次喂回模型，模型看到的历史就变成「一次只调一个工具」，后续会退化成串行调用 | |
| 2 | | 错误自纠错 | • 工具执行报错（参数错、超时、格式错）时，不是直接失败退出，而是把错误信息喂回模型，让模型看懂后重新调用 | P0 |
| | | | • 错误信息必须「可操作」：明确说「email 字段格式错误，期望 xxx@yyy」，而不是甩一个 `422 ValidationError` 给模型猜 | |
| 3 | | 无限循环防护 | • `max_turns` 限制循环迭代次数，防止模型反复调用工具陷入死循环 | P0 |
| | | | • token 预算限制单次任务总 token 消耗，超出即强制停止 | |
| | | | • wall-clock 时间限制单次任务总耗时，防止长任务一直占住资源 | |
| | | | • kill switch 是运行中的紧急停止信号，能在任意时刻打断执行（区别于 max_turns 这类软限制——软限制是「这一轮结束后才判断」，kill switch 是「立刻打断」） | |
| 4 | | 流式事件状态机 | • 把 agent 运行过程抽象成四类事件：thinking（思考中）/ tool_call（发起工具调用）/ tool_result（工具返回）/ final（最终答案） | P0 |
| | | | • runtime 边跑边产出事件，逐事件推给前端，用户能实时看到进度，而不是等全部跑完才一次性返回 | |
| | | | • 事件流通过 SSE 协议传输（单向推送，见选型 0005） | |
| 5 | | checkpoint 断点续跑 | • 每个 Turn 结束后，把会话状态（消息列表、当前进度）序列化成快照持久化 | P0 |
| | | | • 用 `thread_id` 分区，每个会话的状态独立存储，互不干扰 | |
| | | | • 恢复时从最近的 checkpoint 继续，关键是记录「执行到哪一步了」，从而不重复执行已完成的动作 | |
| | | | • InMemory / Redis / Postgres 三种存储语义不同：InMemory 只在内存（重启即丢）、Redis 是 KV 快照（快但弱一致）、Postgres 有事务能存历史、支持 time-travel 回溯（任意回退到历史某一时刻的 checkpoint 重新执行） | |
| | | | • 序列化选型：pickle（有安全风险、版本绑定）/ JSON（安全但类型丢失）/ msgpack（二进制紧凑） | |
| | | | • 对象不可序列化：messages 里的 tool 对象、自定义 dataclass 塞不进 JSON 时，要注册自定义序列化器 | |
| | | | • 向前兼容：checkpoint 格式升级后旧会话数据能读回（schema 版本号 + 迁移） | |
| 6 | | 强制结构化输出 | • 用 `response_format` + `json_schema` 强制模型返回符合 schema 的 JSON，而不是自由文本，便于下游程序直接消费 | P1 |
| | | | • 模型偶尔会返回不合法 JSON，需要校验；校验失败则带着校验错误信息重试 | |
| | | | • 结构化输出 vs function calling 是两种拿到结构化数据的路径，要能说清各自适用场景（前者约束最终输出格式，后者约束工具参数） | |
| 7 | | 上下文压缩 | • 多轮对话会不断膨胀，逼近窗口上限前必须压缩历史 | P1 |
| | | | • 压缩方式是摘要 + 截断，但绝不能破坏 tool_calls 的结构——压缩后模型仍要能正确理解之前发生过哪些工具调用 | |
| | | | • 摘要是把早期对话总结成一段话保留大意，截断是直接丢弃更早的历史，两者可结合使用 | |
| 8 | | 上下文工程 | • 窗口管理：主动决定「什么内容放进来、什么丢弃」，是被设计出来的，而非被动塞满 | P2 |
| | | | • 动态上下文组装：根据当前任务，从多个来源（用户输入、工具结果、检索片段、记忆）拼装本次上下文 | |
| | | | • 工具结果截断：超长的工具输出不能全塞进上下文，要先截断或摘要再喂给模型 | |
| | | | • lost-in-the-middle：模型对上下文中间位置的内容注意力最弱，关键信息要放开头或结尾 | |
| | | | • prompt 缓存：相同前缀的 prompt 命中缓存，省钱又提速 | |
| | | | • 区分「上下文工程」与「prompt engineering」：前者管信息怎么组织进窗口，后者管指令怎么写，这是面试常考的区别点 | |
| 9 | | 意图识别 + 澄清 | • 用户表达不清、指代模糊（「那个」「它」）、需求摇摆时，不能硬猜一个方向就往下做 | P2 |
| | | | • 反问澄清机制：主动追问「你指的是 A 还是 B」来消除歧义，而不是赌 | |
| | | | • 指代消解：把「它 / 那个」还原成具体的指代对象 | |
| | | | • 多轮意图漂移：用户说着说着改了需求，agent 要能察觉并跟随，而不是死守最初意图 | |
| 10 | | Tool/Function calling 协议细节 | • tool_choice 参数：auto（模型自决）/ required（必须调工具）/ none（禁止调工具）/ 指定某个工具，各自语义与使用场景 | P0 |
| | | | • parallel_tool_calls 开关：默认并行，但工具间有依赖或副作用顺序要求时强制串行 | |
| | | | • message 结构合法性：tool 消息必须紧跟对应的 assistant 带 tool_calls 消息，否则 API 报 400；上下文裁剪/压缩时必须保持配对关系 | |
| | | | • arguments 流式增量：tool_call 的 arguments 是分片 delta 累积的，要拼 JSON 而非整段返回 | |
| | | | • tool 参数 JSON schema 设计：required/optional、enum、嵌套对象、约束条件，schema 质量直接影响模型填参正确率 | |
| | | | • finish_reason 四种取值：stop（正常结束）/ tool_calls（要调工具，loop 继续）/ length（token 截断，需特殊处理）/ content_filter（被安全拦截） | |
| | | | • length 截断时结果不完整，要么续写、要么提示模型精简，不能当正常答案返回 | |
| 11 | | 推理模型 reasoning 处理 | • 推理模型（DeepSeek R1 / o1）会先输出一段 reasoning_content（思维链），再输出最终答案，两者要分离处理 | P0 |
| | | | • 流式场景下 reasoning 也是增量输出，要单独作为一类事件推给前端（区别于 #4 的 thinking / tool_call） | |
| | | | • reasoning 的 token 计入成本和上下文窗口，但不应该回填进消息历史（否则历史无限膨胀） | |
| | | | • 非推理模型没有 reasoning_content 字段，模型层要兼容有无该字段的差异 | |
| 12 | | Agent 后端数据模型 | • 核心实体：thread（会话）/ run（一次执行）/ message（消息）/ tool_call（工具调用）/ checkpoint（快照）/ event（事件） | P2 |
| | | | • 字段设计：id、tenant_id/user_id（多租户）、时间戳、状态、版本 | |
| | | | • 索引与外键：按 thread_id、run_id 查询，时间范围查询 | |
| | | | • event 表（event sourcing，事件溯源）：把每次状态变化作为不可变事件追加记录，需要时重放事件序列重建任意时刻的状态，支撑 trace 回放 | |
| | | | • TTL 与清理：会话过期清理、checkpoint 历史保留策略 | |
| 13 | ② 稳定性/降级 | 模型重试 + 退避 + 幂等键 | • 只对瞬态错误重试（429 限流、5xx 服务端故障、网络超时）；4xx 客户端错误（如参数错）重试也没用，直接放弃 | P0 |
| | | | • 指数退避 + jitter：重试间隔按指数增长，并加随机抖动，避免大量请求同时重试造成「惊群」 | |
| | | | • 幂等键：重试可能导致副作用重复执行，用幂等键去重（用唯一标识记录已执行过的请求，重复请求直接返回已有结果，详见 #17） | |
| | | | • 重试会重复计费 token：幂等键防的是「副作用重复」，但 token 已经花出去了，重试本身就重复烧钱 | |
| | | | • 降费手段：缓存模型响应、记录已消费 token、重试次数与总预算挂钩 | |
| 14 | | 熔断 + failover | • 熔断器三态：关闭（正常工作）/ 打开（熔断，快速失败）/ 半开（试探是否恢复） | P1 |
| | | | • 供应商连续失败达到阈值就熔断，不再把请求打给已经挂掉的供应商，给它恢复时间 | |
| | | | • failover：熔断时自动切换到备份模型，保证服务整体可用 | |
| 15 | | 工具超时 + 取消 | • 分层超时：模型调用约 60s、单个工具执行 10-30s、整个 run 一个总超时，各层独立控制 | P1 |
| | | | • 工具卡死（无限等待外部响应）时要能主动取消，而不是让任务无限挂起 | |
| 16 | | 运行状态机 | • 状态集合：created（已创建）/ running（运行中）/ waiting_tool（等待工具执行完）/ waiting_user（等待用户输入，如 HITL 审批）/ retrying（重试中）/ failed / finished / cancelled | P1 |
| | | | • 核心原则：模型只「建议」下一步动作，真正推进状态的是执行层（runtime），这样多轮之间状态才一致、可控 | |
| 17 | | 幂等 + Saga 补偿 | • 真实副作用动作（下单、退款、发通知）重复执行会有实际后果，必须防止重复 | P1 |
| | | | • 幂等键：用 request_id / task_id / operation_id 标识一次操作，重复请求直接返回已有结果，不再执行 | |
| | | | • Saga 事务：多步真实操作正向依次执行，任一步失败则逆序补偿已执行的步骤（把已下的单退掉） | |
| 18 | | 流式中断/取消 | • 用户中途停止或断连，要优雅终止整个执行，而不是粗暴杀进程 | P1 |
| | | | • 取消正在执行的工具，释放协程、网络连接等资源 | |
| | | | • 已经执行过的真实副作用要关联幂等键走补偿，不能留下半截状态 | |
| 19 | | 业务降级兜底 | • 模型挂了 → 降级到模板回复或搜索直出，保证用户至少能拿到一个答案 | P1 |
| | | | • RAG 检索失败 → 降级到 FAQ 规则匹配 | |
| | | | • agent 多轮都失败 → 降级到确定的 workflow 路径（不用模型动态决策） | |
| | | | • 超时 → 返回已经算出的部分结果，而不是空手而归 | |
| | | | • 最终兜底：转人工客服 | |
| 20 | | 并发请求排队 + 异步任务队列 | • 同一个 thread 上长任务还在跑，新请求来了要排队（FIFO，先进先出），等前一个结束再处理，不能并发写坏同一个会话状态 | P1 |
| | | | • 排队策略可选择：排队等待 / 打断当前任务 / 直接拒绝新请求 | |
| | | | • 长任务异步化：HTTP 立即返回一个 task_id，后台继续跑，前端用 SSE 或轮询拉进度 | |
| | | | • 队列实现分两层：进程内 asyncio 队列（简单、零部署）vs 分布式 MQ（Celery / Redis / RabbitMQ / Kafka，多实例场景） | |
| | | | • 并发状态锁：同一个 thread 同一时刻只允许一个任务写状态 | |
| | | | • 幂等去重：同一个请求重试不会重复入队 | |
| | | | • 背压/限流：队列积压到阈值时拒绝或降级，防止拖垮整个服务 | |
| 21 | | 分布式锁 | • 多实例部署下，同一个 thread 的 checkpoint 写、队列消费需要跨进程互斥，进程内锁（#20 的并发状态锁）失效 | P1 |
| | | | • Redis SETNX（SET if Not eXists，仅当 key 不存在时才能设置成功）实现分布式锁，锁要有 TTL + 自动续租，防止持锁进程崩溃导致死锁 | |
| | | | • 释放锁要校验 owner（用唯一 token），防止误删别人的锁 | |
| | | | • 惊群：多个等待者同时抢锁，用队列化等待或随机退避缓解 | |
| 22 | | 限流算法 | • 固定窗口：实现简单但窗口切换瞬间可能流量翻倍 | P1 |
| | | | • 滑动窗口：更平滑但内存占用更高 | |
| | | | • 令牌桶：允许突发，适合 LLM 请求这类突发流量 | |
| | | | • 漏桶：严格匀速，适合下游有硬性吞吐上限的场景 | |
| | | | • 限流维度：per user / per tenant / per model，多级限流组合 | |
| 23 | ③ 安全 | Prompt Injection 防护 | • 致命三合一：不可信输入（用户 / 外部文档）+ 私有数据（系统里的敏感信息）+ 对外通信（工具能联网 / 发消息），三者齐备才构成真正的注入危害 | P1 |
| | | | • 纵深防御：在输入、检索、工具、输出四层都设护栏，单层被绕过还有下一层兜底 | |
| | | | • 外部文档（网页、上传文件）只能当 data 读取，不能当 instruction 执行 | |
| | | | • 越狱（绕过系统指令）和知识库投毒（污染检索数据）都是注入的变体，要一并防 | |
| | | | • 红队 / 对抗测试：主动构造攻击样本，验证防护是否真的有效 | |
| 24 | | 工具沙箱/最小权限 | • 数据库用只读账号，agent 不能随便写库 | P1 |
| | | | • SQL 白名单：只允许 SELECT 等只读语句，禁止 DROP / DELETE | |
| | | | • 命令白名单 + 目录限制：只能执行白名单里的命令，只能访问限定的目录 | |
| | | | • 参数校验：防止 SQL 注入、路径穿越等攻击 | |
| 25 | | HITL 高危操作审批 | • 高危操作（转账、删数据、发邮件）执行前必须人工确认 | P1 |
| | | | • 用 needs_approval 标记这类操作，runtime 检测到就挂起 | |
| | | | • 挂起后等待人工批准或拒绝，绝不自动执行 | |
| | | | • 挂起点要持久化（记录挂起在哪个 tool_call、审批上下文），审批后从断点恢复执行，而非从头重跑 | |
| | | | • 审批超时 / 拒绝的处理：拒绝要回填失败原因给模型，超时要走降级路径 | |
| 26 | | 敏感信息脱敏 | • PII（手机号、身份证、邮箱）在进入日志前先脱敏 | P1 |
| | | | • 日志和 trace 里绝不能出现 API Key、token 等凭据 | |
| | | | • 结构化脱敏：按字段类型脱敏（知道这是个手机号就按手机号规则打码），而不是靠正则碰运气 | |
| 27 | | 审计/合规轨迹 | • 审计要求：合规官能根据记录，重构出 agent 当时的完整推理路径（为什么这么回答、调了哪个工具、访问了哪条数据） | P1 |
| | | | • 每次工具调用、每次数据访问都要留审计记录 | |
| | | | • 全链路可回溯：从输入到输出，每一步都能追溯 | |
| 28 | | 数据合规删除权 | • GDPR 被遗忘权：用户要求删除时，其数据（对话、记忆、日志、审计记录）要能物理删除，而非仅软删除标记 | P2 |
| | | | • 记忆系统 #33 的软删除与合规删除是两回事：软删除为可追溯，合规删除为彻底移除 | |
| | | | • 删除的传播：删除用户数据要级联到衍生数据（向量、checkpoint 快照、trace） | |
| | | | • 删除后要能证明已删（审计删除操作本身），不能「删了但检索还能搜到」 | |
| 29 | | 输出护栏 + 幻觉控制 + 引用溯源 | • Output guardrail：输出有害内容过滤、格式校验、二次 PII 检查、防越权信息泄露（数据出逃） | P1 |
| | | | • 幻觉成因：训练数据偏差、采样随机、上下文不足、长尾知识 | |
| | | | • 幻觉缓解：grounding（强制基于检索结果回答）、低置信转人工、明确「不知道」的拒答策略 | |
| | | | • 引用溯源（citation）：RAG 回答带来源、逐句引用，便于用户核查，也是落地 RAG 应用的标配 | |
| | | | • 输出侧也防注入：工具返回内容被模型当指令执行、外部数据污染输出，输出侧同样要有护栏 | |
| 30 | | SSRF + RBAC + 密钥管理 | • SSRF（Server-Side Request Forgery，服务端请求伪造）：攻击者诱导服务端去请求攻击者指定的地址；agent 的 fetch URL 类工具（网页搜索、文件下载）就是攻击面——若不对地址做校验，攻击者可以让服务端去访问内网地址，绕过防火墙窃取内网信息。防护：校验目标地址、禁止内网 IP / 私网网段 | P1 |
| | | | • RBAC（Role-Based Access Control，基于角色的访问控制）/权限模型：不只是 #32 的多租户隔离，还有角色（谁有权调高危工具） | |
| | | | • 密钥管理：API Key 不进代码、运行时注入、KMS（Key Management Service，密钥管理服务）、定期轮换 | |
| 31 | ④ 记忆 | 记忆分层（短期 / 情景 / 语义 / 程序性四层） | • 短期记忆：当前对话的上下文 | P2 |
| | | | • 情景记忆：带时间戳的历史事件记录（某年某月用户说过什么） | |
| | | | • 语义记忆：向量化的知识（「用户偏好 X」） | |
| | | | • 程序性记忆：行为模式（「用户遇到问题会先查 FAQ」） | |
| | | | • 记忆冲突时用时间衰减（recency weighting）解决，新记忆权重高于旧记忆 | |
| | | | • 记忆废弃用软删除标记，不物理删除，保留可追溯性 | |
| 32 | | 多租户隔离 | • 每条数据、每条记忆都带 tenant_id / user_id 元数据 | P2 |
| | | | • 检索时强制 `WHERE user_id = ...` 过滤，从根上杜绝跨租户数据泄露 | |
| | | | • 这是多租户 SaaS 的硬性安全要求，不能靠「相信模型不乱看」 | |
| 33 | | 记忆机制（写入 / 检索 / 遗忘） | • 写入决策：谁决定写记忆（显式规则 vs 记忆 agent），写什么（事实 vs 偏好 vs 事件） | P2 |
| | | | • 检索策略：语义 + 时间 + 重要度加权，取 top-k | |
| | | | • 遗忘机制：recency decay（时间衰减）、容量淘汰（LRU，Least Recently Used，最近最少使用 / 重要度排序） | |
| | | | • 记忆冲突解决：新记忆覆盖旧记忆，软删除保留可追溯 | |
| 34 | ⑤ 成本 | 成本追踪 | • 按 task 归因成本（一次任务总共花了多少钱），而不是只盯着单个 token 价格 | P2 |
| | | | • token 归因：哪一步、哪个工具、哪个模型各花了多少 token，能拆解清楚 | |
| | | | • 预算硬上限：per user / per tenant 设预算，超了就拒绝，防止单用户烧穿 | |
| | | | • 成本告警：成本出现尖峰时告警 | |
| 35 | | Token 计量与成本预估 | • tiktoken / BPE 原理：token 不是字符，中文 / 代码 / 特殊符号的 token 化差异大 | P1 |
| | | | • 不同模型 tokenizer 不同，token 数不同，成本核算要按实际模型算 | |
| | | | • 请求前预估 token（输入消息 + 预留输出窗口 + 工具结果空间）做预算硬截断（关联 #3） | |
| | | | • 预估 vs 实际：流式输出实际 token 以 usage 返回为准，预估用于提前止损 | |
| 36 | | 模型分级路由 + 语义缓存 | • 分级路由：简单步骤（分类、抽取）用便宜模型，难推理用强模型，从而省成本 | P2 |
| | | | • 语义缓存：高频相似问题命中缓存直接返回，不重复调 LLM | |
| | | | • 缓存穿透：查询不存在的 key 每次都打到后端，用空值缓存或布隆过滤器兜底（布隆过滤器：用少量内存近似判断「key 是否存在」的概率型数据结构，判断不存在就直接返回，不查后端） | |
| | | | • 缓存击穿：热点 key 过期瞬间大量并发打后端，用互斥锁 / 热点永不过期 | |
| | | | • 缓存雪崩：大量 key 同时过期，用随机 TTL 错开过期时间 | |
| | | | • prompt 缓存：相同前缀命中缓存 | |
| | | | • 模型网关：统一治理多模型的路由、限流、计费 | |
| 37 | | Batch API 降本 | • Batch API：把多个请求打包异步处理，通常可降约 50% 成本，但延迟高（结果数小时到一天返回） | P2 |
| | | | • 适用场景：离线评估跑分、批量数据标注 / 分类、数据飞轮 #60 的批量处理 | |
| | | | • 不适用：实时交互（客服、对话），延迟敏感场景 | |
| | | | • batch 结果回填：任务 id 轮询结果，失败重试，与实时请求的幂等键统一 | |
| 38 | ⑥ 可观测 | 日志结构化 + 全链路关联 | • 结构化日志：JSON 格式，可机器解析、可检索，而非自由文本 | P1 |
| | | | • request_id / trace_id / thread_id 贯穿：一次请求从入口到 LLM 调用到工具执行，都能关联起来 | |
| | | | • 日志分级（DEBUG / INFO / WARNING / ERROR）+ 关键上下文（user_id、model、耗时、token） | |
| | | | • 脱敏前置：日志进入前先脱敏（关联 #26），凭据绝不落日志 | |
| | | | • 日志聚合与检索：接入日志平台（ELK / Loki），支撑 #41 trace 回放和 #59 调试 | |
| 39 | | 指标 + 告警 | • 核心指标：成功率、重试率、升级率（escalation rate）、p95 延迟（95% 的请求在该耗时以内完成，衡量尾部延迟） | P2 |
| | | | • 工具层面：每个工具的成功率、护栏拦截率（guardrail block rate） | |
| | | | • 告警：成本尖峰、工具错误尖峰、循环检测触发等异常告警 | |
| 40 | | 版本化 | • prompt / model / tool 都要版本化，能知道某次运行用的是哪个版本 | P2 |
| | | | • trace 里记录版本号，出问题能定位到具体版本 | |
| | | | • 灰度发布 + 回滚：新版本小流量验证，坏了能快速回滚 | |
| 41 | | trace 标准与工具 | • OpenTelemetry / Langfuse / LangSmith / OpenInference 等 trace 工具 | P2 |
| | | | • span 结构：run → turn → tool_call 层级 | |
| | | | • 日志 / 指标 / trace 三支柱（关联 #39-40） | |
| | | | • trace 的可视化与检索：按 thread / run 检索，定位慢调用与错误 | |
| 42 | ⑦ 多 agent | 多 agent 拓扑与状态共享 | • 拓扑选择：Supervisor（一个主 agent 调度多个子 agent）vs P2P（agent 之间对等协商） | P2 |
| | | | • 共享黑板 vs 私有记忆：多 agent 通过共享状态协作，还是各自维护私有记忆 | |
| 43 | | 多 agent 协作与纠错 | • Critic 模式：一个 agent 独立评审另一个 agent 的输出，互相纠错 | P2 |
| | | | • subagent 隔离：子 agent 失败不能拖垮主 agent | |
| 44 | | 多 agent 控制流风险 | • handoff 循环检测：A 转给 B、B 又转回 A，要检测并打破这种循环 | P2 |
| | | | • 死锁预防：多个 agent 互相等待对方结果导致死锁 | |
| 45 | ⑧ RAG | 知识注入选型（RAG / 微调 / 长上下文） | • RAG：知识频繁更新、需引用溯源、按需检索，便宜、灵活、可解释 | P1 |
| | | | • 微调（Fine-tuning）：固化风格 / 格式 / 领域术语、稳定输出、降低每次推理成本，但贵、需训练数据、知识更新需重训 | |
| | | | • 长上下文：一次性少量文档、跨文档推理、无检索误差，但贵、慢、受窗口上限 | |
| | | | • 三者组合：RAG 兜底知识广度 + 微调固化行为 + 长上下文处理局部推理 | |
| 46 | | RAG 数据摄取 | • 文档解析：PDF / Word / HTML / Markdown 的结构化提取（pypdf / unstructured / python-docx） | P1 |
| | | | • 表格抽取：PDF 表格、扫描件 OCR，是真实项目的高频坑 | |
| | | | • 数据清洗：去噪、去重、格式归一化、无效内容过滤 | |
| | | | • 元数据提取：标题、章节、来源、时间戳，供后续过滤（#32 多租户）和引用溯源（#29） | |
| | | | • 摄取结果质量决定检索上限：垃圾进垃圾出，清洗比调 chunk 参数更根本 | |
| 47 | | RAG 检索工具 | • chunk 分块：按 size / overlap 切文档，太小丢上下文、太大检索不准，要调优 | P1 |
| | | | • 父子索引：用小 chunk 做检索、用大 chunk（父块）喂给模型，兼顾检索精度和上下文完整性 | |
| | | | • embedding 选型：不同向量模型的向量质量不同，直接影响检索效果 | |
| | | | • 混合检索：BM25（经典的关键词相关度排序算法）关键词检索 + 向量语义检索加权融合，弥补纯向量对精确关键词不敏感 | |
| | | | • rerank：粗召回后用重排模型精排，提高最相关结果排前面的概率 | |
| | | | • query rewrite：检索前改写或扩展用户问题，提升召回率 | |
| | | | • 增量更新：文档变化时增量更新索引，不用每次全量重建 | |
| | | | • embedding 模型升级后向量空间不兼容，旧向量不能和新查询混用，需全量 re-embedding 或用版本号隔离 | |
| 48 | | RAG 检索评估 | • 评估指标：命中率（hit rate）、MRR（Mean Reciprocal Rank，平均倒数排名——正确答案排第 1 得 1 分、排第 2 得 1/2 分，衡量正确结果排得有多靠前）、忠实度（faithfulness）、幻觉率 | P2 |
| | | | • 三层排查：检索不到（召回问题）/ 检索错（排序问题）/ 生成错（生成问题），先定位是哪一层出的问题 | |
| | | | • badcase 集：积累失败案例做回归测试 | |
| | | | • 怎么证明 RAG 变好了：用指标对比，而不是拍脑袋说「感觉准了」 | |
| 49 | | RAG 前沿深化（GraphRAG / RAGAS / 向量库选型） | • GraphRAG：知识图谱增强检索，处理多跳问题与全局问题 | P2 |
| | | | • RAGAS 评估框架：faithfulness（忠实度）/ answer relevancy（答案相关性）/ context precision（上下文精确率，检索结果中有多少被回答用到）/ context recall（上下文召回率，回答所需信息有多少被检索到） | |
| | | | • 向量库选型：pgvector / FAISS / Milvus / Chroma 的对比（性能 / 规模 / 运维） | |
| | | | • 向量库 metadata filter：按 tenant_id 等元数据过滤（关联 #32） | |
| 50 | ⑨ 能力扩展 | MCP 集成 | • MCP client：连接外部 MCP server，把外部提供的工具接入 agent | P2 |
| | | | • MCP server 开发：把自己的工具封装成 MCP server，供别的 agent 调用 | |
| | | | • 三种传输方式：stdio（本地进程标准输入输出）/ SSE（早期远程传输）/ Streamable HTTP（现行推荐标准，替代 SSE、解决长连接管理问题） | |
| | | | • 三类能力，不只是 tools：tools（工具）/ resources（只读资源，如文件内容、数据库记录）/ prompts（提示词模板） | |
| | | | • 生命周期：initialize（握手、协商协议版本与能力）→ tools/list（发现工具）→ tools/call（调用工具） | |
| | | | • sampling：MCP server 可以反向请求 client 调用 LLM（agent 当 client 时，server 反过来让 agent 帮忙生成内容） | |
| | | | • 工具发现 / 权限 / 多 server 路由：多个 MCP server 的工具如何被发现、授权、路由，同名工具如何仲裁 | |
| 51 | | Agent Skills | • SKILL.md 格式：技能的元数据 + 指令的固定结构 | P2 |
| | | | • 渐进式披露三级加载：metadata（先看名字 / 描述）→ instructions（用到时加载）→ resources（深入时加载），省 token | |
| | | | • Skill 路由：skill 多时（几十上百）不能全量注入，用分层路由——L1 规则粗筛（关键词 / 意图）→ L2 语义召回（embedding 相似度取 top-k）→ L3 模型终选（在 top-k 里拍板） | |
| | | | • description 质量决定路由准确性：description 是路由的「索引」，要写清何时用、解决什么、能力边界、排除场景 | |
| | | | • 选错检测与冲突仲裁：模型调了不相关 skill 要能发现并让它重选；多个 skill 都相关时如何仲裁 | |
| | | | • 版本化 / 热更新 / 依赖 / 权限：skill 更新后运行中的 agent 切新版；skill 之间的依赖加载顺序；某些 skill 需特殊权限 | |
| | | | • 与 MCP / function calling 的区别：MCP 提供「连接」（怎么调到工具），Skill 提供「知识」（怎么做好某类任务），两者互补 | |
| 52 | | A2A（Agent-to-Agent）协议 | • Agent Card：声明 agent 的能力、端点、鉴权方式 | P2 |
| | | | • 任务与消息：A2A 的核心交互单元 | |
| | | | • 传输：HTTP + JSON-RPC（基于 JSON 格式的远程过程调用协议）/ SSE | |
| | | | • 与 MCP 的区别：MCP 是 agent ↔ 工具，A2A 是 agent ↔ agent | |
| | | | • 多 agent 协作场景下的发现、寻址、编排 | |
| 53 | | 多模态 agent | • 视觉输入：图像理解、OCR、截图分析 | P2 |
| | | | • 多模态工具：图像生成、音视频处理工具挂载 | |
| | | | • 多模态消息结构：image 字段的 message 格式 | |
| | | | • 应用场景：客服（用户发截图）、computer use（让模型像人一样操作电脑完成任务的范式） | |
| 54 | | 代码解释器 / 工具执行环境 | • E2B（云端代码执行沙箱服务）/ Docker 沙箱 / 子进程隔离（关联 #24 沙箱） | P2 |
| | | | • 代码解释器：让 agent 生成并执行代码（数据分析、文件处理） | |
| | | | • 执行结果回填：stdout / stderr / 文件产物回填给模型 | |
| | | | • 资源限制与超时：CPU / 内存 / 磁盘限制，防止沙箱被拖垮 | |
| 55 | ⑩ 架构与策略 | Planning 范式 | • ReAct：边推理边行动（think → act → observe 循环） | P2 |
| | | | • Plan-and-Execute：先整体规划出步骤，再逐步执行 | |
| | | | • Reflection：执行后自我反思，发现不足再改进 | |
| | | | • Replan：执行中发现计划有问题，重新规划 | |
| 56 | | Workflow vs Agent | • workflow 适用：路径确定、可控、便宜的固定步骤编排 | P2 |
| | | | • agent 适用：路径不确定、需要模型动态决策的场景 | |
| | | | • 实践原则：用 workflow 做骨架兜底，局部不确定环节才用 agent | |
| 57 | | Agent 框架对比 | • LangChain / LangGraph / DeepAgents / 手写 各自的 tradeoff（权衡取舍） | P2 |
| | | | • 何时用框架、何时自研：核心诉求是控制力还是开发速度 | |
| | | | • LangGraph 的图模型 vs 手写 while 循环的差异与适用场景 | |
| | | | • 面试表达：能讲清「框架替你做了什么、为什么那么设计」是理解深度的分水岭 | |
| 58 | ⑪ 评估与迭代 | Agent 评估体系 | • 评估维度：任务成功率（端到端做完没有）、工具选择正确率（该用哪个工具用对没有）、回答质量（相关性 / 忠实度 / 无害性）、轨迹质量（有没有走弯路、反复调同一个工具） | P2 |
| | | | • LLM-as-judge：用 LLM 当裁判给 agent 输出打分，省人工但 LLM judge 有系统性偏差——位置偏差（偏爱排前面的）、长度偏差（偏爱长的）、自我偏好（偏爱自己模型的输出） | |
| | | | • 缓解 LLM judge 偏差：多 judge 交叉打分、交换候选顺序、用结构化 rubric（明确打分标准而非自由发挥） | |
| | | | • GoldenSet 构建：评估集要覆盖难易分布和场景分布，标注成本是瓶颈，常见做法是线上采样 + 人工精标 | |
| | | | • 离线评估 vs 在线评估：离线用固定评估集跑回归，快但与真实分布有偏差；在线看真实流量指标，准但慢 | |
| 59 | | Agent 调试 | • trace 回放：agent 出问题时，从记录的 trace 恢复执行现场，逐步回看每一轮模型看到了什么、做了什么决定 | P2 |
| | | | • 决策归因：模型为什么这么选？回看当时的上下文（prompt、历史、工具结果），定位是 prompt 问题、工具结果问题还是模型本身问题 | |
| | | | • 为什么 agent 难调试：普通程序行为确定，agent 行为有随机性（温度采样），同一输入可能不同输出，所以要靠 trace 而非复现 | |
| 60 | | 数据飞轮 | • badcase 收集：用户点踩、人工标注、自动检测（工具报错、护栏拦截）都是 badcase 来源 | P2 |
| | | | • 归类分析：把 badcase 归因到「检索问题 / 生成问题 / 工具问题 / 意图问题」，而不是笼统说「答错了」 | |
| | | | • 改进闭环：根据归因对症下药——调 prompt、改工具描述、加 skill、补数据 | |
| | | | • 回归验证：改进后跑评估集确认没退化，badcase 进评估集防止再犯 | |
| | | | • 这是 agent 持续变好的核心方法论：没有数据飞轮的 agent 是一次性的，有数据飞轮的才越用越好 | |
| 61 | ⑫ 测试 | mock 与确定性 | • mock LLM：固定返回、脚本化序列返回（第一次调 A 第二次调 B）、录制回放（record / replay），让 loop 测试不依赖真实模型 | P0 |
| | | | • 确定性测试：temperature=0 + seed 固定 + 注入随机源，保证同输入同输出，否则测试断言会 flaky | |
| 62 | | 分层测试与轨迹断言 | • 分层测试：单测（fake model + fake tool 测 loop 分支）、集成（真实 LLM + 打桩外部工具）、E2E（真实链路跑通） | P0 |
| | | | • golden 轨迹断言：不只断言最终答案，还断言「调了哪些工具、调用顺序、参数」是否符合预期轨迹 | |
| | | | • 非确定性测试：多次运行统计通过率，而非单次通过 | |
| 63 | | 快照/契约测试 + 测试边界 | • 快照/契约测试：对 checkpoint、事件流做快照对比，防止回归；契约测试（contract test）验证接口双方按约定格式交互 | P0 |
| | | | • 与 #58 评估体系的区别：测试测「代码对不对」（确定性，可 mock），评估测「输出好不好」（非确定性，用 LLM-as-judge） | |
| 64 | ⑬ 工程化与部署 | 无状态化与水平扩展 | • 无状态化设计：状态外置到 Redis/Postgres，进程可任意水平扩（#5 双实现的延伸价值） | P2 |
| | | | • 跨实例会话：sticky session（粘性会话，把同一用户的请求固定路由到同一实例）vs 状态外置两种方案权衡 | |
| | | | • 优雅停机、健康检查、滚动发布 | |
| | | | • 进程内队列（#20）在多实例下的局限与升级分布式 MQ 的路径 | |
| | | | • 优雅停机的任务 drain：发布 / 重启时正在跑的 agent 任务（几十秒到分钟级）怎么办——等它跑完 / 迁移到新实例 / 中断 + 断点续跑 | |
| | | | • agent 任务长且有状态，直接 kill 会丢进度，要有 drain 策略（SIGTERM → 停止接新请求 → 等存量任务完成或迁移） | |
| 65 | | asyncio 并发模型 | • 事件循环、async/await、coroutine 与 task 的区别 | P0 |
| | | | • 阻塞 IO 陷阱：sync requests 会阻塞事件循环，必须用 httpx.AsyncClient | |
| | | | • asyncio.gather 并发执行工具，异常处理（return_exceptions） | |
| | | | • GIL（Python 全局解释器锁，同一时刻只有一个线程执行 Python 字节码）与线程/进程模型：何时用 asyncio、何时用线程池/进程池 | |
| | | | • 取消语义：asyncio.Task.cancel 与 CancelledError 的传播，对应 #18 流式中断 | |
| 66 | | 延迟优化 | • TTFT（首 token 延迟）vs 总延迟：流式场景用户感知的是 TTFT | P2 |
| | | | • 流式感知延迟 vs 完整结果延迟 | |
| | | | • 并行 vs 串行工具的延迟权衡：并行降低总延迟但增加单工具复杂度 | |
| | | | • 延迟优化手段：缓存命中、模型分级、减少工具往返 | |
| 67 | | 工程化底座（连接 / 配置 / 迁移 / 依赖 / 异常 / 泄漏） | • 连接管理：httpx AsyncClient / DB / Redis 连接池的生命周期——复用、泄漏、池耗尽 | P2 |
| | | | • 配置管理：多环境（dev / prod）、secrets 注入、配置错误排查（12-factor，云原生应用十二条原则，核心之一是配置存于环境变量而非代码） | |
| | | | • 数据库 schema 迁移：checkpoint / thread 表结构演进、迁移工具（alembic）、向前兼容 | |
| | | | • 依赖管理：依赖锁定、冲突解决、SDK 升级的 API 不兼容 | |
| | | | • 内存 / 资源泄漏：长运行进程的 asyncio task 泄漏、缓存无界增长、连接未关闭 | |
| | | | • 工程异常处理与错误上报：异常分类、错误日志、Sentry 聚合（区别于 #2 的 agent 错误自纠错） | |
| 68 | ⑭ 横切基础点 | 横切点（贯穿所有层、不单独属于某一层的基础技术点） | • 工具描述优化：工具描述要写给「没见过系统的初级工程师」看——明确参数含义、副作用、失败模式、使用时机，模型才能正确选择何时调用（落地 tool.py，P0） | P0-P1 |
| | | | • 温度/采样控制 + 可复现性：temperature / top_p 可配置；seed 固定后同输入得到同输出，便于复现问题（落地 model 层，P0） | |
| | | | • 限流/背压：上游 QPS 限制；队列积压时拒绝或降级，保护服务不被拖垮（落地 server 接入层，与 #20 交叉，P1） | |
| 69 | | Prompt Engineering | • system prompt 设计：角色、约束、能力边界、输出格式 | P0-P1 |
| | | | • few-shot / CoT（Chain-of-Thought，思维链）/ ReAct 模板设计 | |
| | | | • 动态 few-shot 选择：按语义相似度从示例库挑最相关的 few-shot 注入 | |
| | | | • prompt 模板版本化与 A/B（关联 #40） | |
| | | | • 与 #8 上下文工程的区别：prompt 工程管「指令怎么写」，上下文工程管「信息怎么组织进窗口」 | |
| 70 | | 工具设计方法论 | • 工具粒度：一个工具只做一件事，粒度太粗难被正确调用、太细增加选择负担 | P0 |
| | | | • 工具数量与准确率 tradeoff：工具越多模型越容易选错 | |
| | | | • 工具命名与描述（关联 #68 工具描述优化） | |
| | | | • 工具的参数设计：参数越少越好，复杂参数用结构化 schema | |

### 难点编号分布

| 类别 | 编号 | 数量 |
|------|------|------|
| ① 核心循环 | 1-12 | 12 |
| ② 稳定性/降级 | 13-22 | 10 |
| ③ 安全 | 23-30 | 8 |
| ④ 记忆 | 31-33 | 3 |
| ⑤ 成本 | 34-37 | 4 |
| ⑥ 可观测 | 38-41 | 4 |
| ⑦ 多 agent | 42-44 | 3 |
| ⑧ RAG | 45-49 | 5 |
| ⑨ 能力扩展 | 50-54 | 5 |
| ⑩ 架构与策略 | 55-57 | 3 |
| ⑪ 评估与迭代 | 58-60 | 3 |
| ⑫ 测试 | 61-63 | 3 |
| ⑬ 工程化与部署 | 64-67 | 4 |
| ⑭ 横切基础点 | 68-70 | 3 |

## 面试要点导航

把难点按面试主题归类，复习时快速定位：

| 面试主题 | 对应难点 |
|----------|----------|
| 手写 agent loop 完整实现 | #1-5, #10-11 |
| 测试（mock / 确定性 / 分层 / 轨迹断言 / 快照） | #61-63 |
| 生产可靠性（重试 / 熔断 / 超时 / 幂等 / 降级 / 限流 / 分布式锁） | #13-22 |
| 并发与异步（请求排队 / 长任务异步化 / 分布式锁 / asyncio） | #20-21, #65 |
| 安全（注入 / 沙箱 / HITL / 脱敏 / 审计 / 合规删除 / 输出护栏 / SSRF） | #23-30 |
| 记忆系统（四层记忆 / 多租户隔离 / 记忆机制） | #31-33 |
| 成本优化（追踪 / token 计量 / 分级路由 / 缓存 / batch） | #34-37 |
| 可观测（日志 / 指标 / 告警 / 版本化 / trace） | #38-41 |
| 多 agent 协作（拓扑 / 协作 / 控制流 / A2A） | #42-44, #52 |
| RAG（知识注入选型 / 数据摄取 / 检索 / 评估 / 前沿） | #45-49 |
| 协议扩展（MCP / Skills） | #50-51 |
| 架构设计（Planning / Workflow vs Agent / 框架对比） | #55-57 |
| 上下文与 prompt 工程（窗口管理 / lost-in-the-middle / prompt 工程） | #6-9, #69 |
| 数据模型与部署（表结构 / 无状态化 / 延迟优化 / 工程底座） | #12, #64, #66-67 |
| 多模态与执行环境（多模态 / 代码解释器） | #53-54 |
| 评估与迭代（评估体系 / 调试 / 数据飞轮） | #58-60 |

## 目录结构

```text
handcraft_agent/
├── README.md                   # 项目说明 + 70 点难点清单 + 面试导航
├── CONTEXT.md                  # 领域术语表（glossary）
├── handcraft_agent/            # 框架包（agent runtime，业务无关）
│   ├── __init__.py
│   ├── model.py                # P0  ChatModel 协议 + httpx/openai 双实现 + reasoning 兼容
│   ├── tool.py                 # P0  @tool 装饰器 + JSON schema 生成
│   ├── loop.py                 # P0  手写 agent loop（并行/纠错/循环防护）
│   ├── stream.py               # P0  流式事件（SSE）
│   ├── retry.py                # P0  重试 + 退避 + 幂等键
│   ├── checkpoint/             # P0  base / memory / redis / postgres + 序列化协议
│   ├── guard.py                # P1  输入/输出护栏 + 脱敏 + HITL + 审计
│   ├── ratelimit.py            # P1  限流算法（固定/滑动窗口 + 令牌桶/漏桶）
│   ├── lock.py                 # P1  分布式锁
│   ├── rag/                    # P1  数据摄取 / 检索 / 评估
│   ├── memory/                 # P2  四层记忆 + 多租户隔离
│   ├── cost/                   # P2  成本追踪 + token 计量 + 分级路由 + 缓存 + batch
│   ├── observability/          # P2  日志 + 指标 + trace + 版本化
│   ├── multiagent/             # P2  Supervisor / P2P / handoff
│   ├── mcp/                    # P2  MCP client + server
│   ├── skills/                 # P2  Agent Skills
│   └── eval/                   # P2  评估 + 调试 + 数据飞轮
├── server/                     # P1  FastAPI 服务（SSE 接口 + 任务队列）
├── demo/                       # P1  智能客服 demo（Ticket / Escalation）
└── tests/                      # P0  单元 / 集成 / E2E 测试
```

## 快速开始

> 各阶段运行入口随实现补充，规划如下：

| 阶段 | 运行入口 | 说明 |
|------|---------|------|
| P0 | `python -m handcraft_agent.cli` | CLI 跑通 loop（不依赖 server），checkpoint 断点续跑 + 单测 |
| P1 | `python -m server.main` + 前端 `npm run dev` | FastAPI + SSE，客服 demo 端到端 |
| P2 | 各模块按需启动 | RAG 索引构建、评估集跑分、trace 面板 |

## 文档索引

- [CONTEXT.md](CONTEXT.md) — 领域术语表
