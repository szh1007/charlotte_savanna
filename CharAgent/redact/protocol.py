"""`Redactor` 协议: 日志出口上的一道工序 (difficulties #26 / #38).

一句话理解: 谁要往日志 (或任何会留痕的地方) 写东西, 就先问它一遍 —— 这段文字 /
这个字典里有没有不该留的值.

**两个方法分开, 不是重复** (#26 的原话是「按字段类型脱敏而非正则碰运气」):

- `redact_fields` —— **有结构的数据** (正要写进日志的那个 dict): 按**业务声明的
  字段路径**打码. 这是主手段: 知道它是什么, 就按它是什么打.
- `redact_text` —— **自由文本** (拼好的一句话 / 工具返回的正文 / 上游的错误体):
  按**形状**认. 这是最后一道, 兜不住的如实留着 (不假装).

为什么自由文本那条只能是「最后一道」: 在没有字段知识的地方, 正则就是在碰运气 ——
`13800000003` 是手机号还是某个编号的一部分, 只能按形状猜. 所以它**不假装兜得住**
(纪律写在 `rules` 的模块 docstring 里).

**谁做什么**: 框架给协议 + 通用规则 (手机号 / 邮箱 / 身份证 / 银行卡是跨业务的形状),
**业务注册自己的字段路径** —— 哪些格子敏感只有业务自己知道 (框架不认识业务).

大白话版: 这是个「打码员」的统一长相. 规矩是框架的 (手机号长什么样), 名单是业务的
(我们家的哪个字段是手机号). 出口在哪一侧, 就在哪一侧调用它.
"""

from __future__ import annotations

from collections.abc import Mapping
from typing import Any, Protocol


class Redactor(Protocol):
    """把敏感值换成打码后的样子 (两个方法各管一类输入, 见模块 docstring).

    形状上「有个两个方法就算」, 不继承基类 (与框架里别的协议同款: `ChatModel` /
    `EventSink` 都是这么定的).
    """

    def redact_text(self, text: str) -> str:
        """自由文本打码 (**最后一道**: 规则型, 兜不住的如实留着).

        Args:
            text: 要写出去的那段文字.

        Returns:
            str: 打码后的文字 (没认出任何东西时, 逐字返回原文).
        """
        ...

    def redact_fields(self, data: Mapping[str, Any]) -> dict[str, Any]:
        """按声明打码 (**主手段**: 知道它是什么, 就按它是什么打).

        Args:
            data: 有结构的一份数据 (要写日志的那个 dict).

        Returns:
            dict[str, Any]: 打码后的**新**字典 —— 递进来的那一份不动 (它常常是还要
            落库 / 还要发给别人的原始数据).
        """
        ...
