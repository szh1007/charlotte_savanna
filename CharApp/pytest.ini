[pytest]
testpaths = tests
# `..` = 仓库根 (`import CharApp.minimall`); `../CharAgent/tests` = 复用框架的测试
# 替身 (MockLLM / 轨迹断言) —— 业务侧不抄一份「假大脑」, 见 PRD §5 测试接缝一.
pythonpath = .. ../CharAgent/tests
asyncio_mode = auto
