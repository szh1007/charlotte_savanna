# 08 — 前端解析结果卡 + 任务面板

**What to build:** 用户在解析结果卡中选择清晰度档位发起下载（免费档锁定档位带 🔒 标识），创建的任务出现在任务面板并实时显示进度（SSE 推送，无需刷新）；完成后可一键下载文件或复制交付直链。

**Blocked by:** 02 — 下载 + 交付直链链路, 07 — 前端工程 + 主题 + Hero 解析区

**Status:** resolved

**验收标准：**
- [x] 解析结果卡：封面 + 标题 + 平台徽章 + 时长 + 清晰度下拉（免费锁定档位带 🔒 标识）→「开始下载」按钮
- [x] 点击开始下载 → 创建下载任务 → 任务出现在任务面板
- [x] 任务卡片：封面缩略图 + 进度条 + 状态徽章（排队中 / 下载中 / 已完成 / 失败 / 已过期）+ 失败原因展示
- [x] 通过 SSE 实时更新进度与状态（无刷新、无轮询）
- [x] 完成后显示「下载到手机/电脑」按钮（打开交付直链）+ 「复制链接」按钮
- [x] 页面恢复初始状态后任务列表可从接口恢复（GET /api/tasks）
- [x] 浏览器端到端通过（真实后端：解析 → 下载 → 实时进度 → 直链下载）

## Comments

- 2026-08-19: T08 完成。实现: ResolveResult.vue 接入「开始下载」按钮 (防重复点击 + 全档位锁定场景禁用 + 创建失败错误提示) + api/client.js 新增 createDownload/fetchTasks + 新建 TaskPanel.vue (封面缩略图/8 态状态徽章着色/渐变进度条/失败原因与 expired 提示/completed 显示「⬇ 下载到手机/电脑」直链 + 「复制链接」(clipboard API + 2s 反馈)) + Home.vue 整合 (创建成功本地构造任务卡即时上卡, 元信息来自解析结果; EventSource 订阅 /api/events 的 task-update 事件实时合并状态, 无轮询; onMounted 先 fetchTasks 恢复下载任务 (过滤 kind=download) 再连 SSE, 顺序保证快照不覆盖元信息; 任务面板只展示下载任务, resolve 类任务不混入)。验证: `npm run build` 通过 (73.94KB JS); 后端 pytest 53 passed 回归; 浏览器 E2E 16 项全过 (Chrome headless + CDP 直驱真实后端: 打开页面 → 空状态 → 粘贴 B 站链接 BV1GJ411x7h7 → 解析 5 档 (2 档 🔒 disabled) → 开始下载 → SSE 推到已完成 100% → 直链取回 9.2MB 有效 MP4 → 刷新后任务恢复, 截图留存)。/code-review 双轴审查通过, 修复: 6 处超长行折行 / completed→100% 双写抽取 widthPercent / 创建失败 (429/400) 时后端任务已落库 failed, catch 分支防抖补拉列表使失败任务上卡与刷新后展示一致 / SSE 未知 task_id 竞态事件 (POST 响应前到达的 queued 事件) 触发防抖刷新自愈 / 全档位锁定 (会员专属视频) 时禁用「开始下载」避免必然 400。提交: `e1464c2`。
