# 17-P1-7 — HITL 审批 + 敏感信息脱敏 + 审计轨迹

**What to build:** HITL 高危操作审批全流程：工具执行到高危动作（退款/赔付）→ 挂起（needs_approval）→ 挂起点 checkpoint 落盘 → approval_required SSE 事件 → 审批台批准后从挂起 checkpoint 恢复执行（不重跑）/ 拒绝后失败原因回填模型继续推理 / 超时（默认 15 分钟）自动拒绝走降级（#25）。敏感信息 PII 结构化脱敏（日志/trace 前置 #26）。审计轨迹：每次工具调用/数据访问写入 audit_logs（脱敏后 #27）。

**Blocked by:** 11, 12

**Status:** ready-for-agent

- [ ] 挂起 → 落盘 → 审批恢复（不重跑，验证已执行步骤不重复）（#25）
- [ ] 拒绝 → 失败原因回填模型继续推理（#25）
- [ ] 超时降级：默认 15 分钟自动拒绝 + 降级路径（#25）
- [ ] PII 结构化脱敏：日志/trace 中 PII 与 API Key 脱敏（#26）
- [ ] audit_logs：每次工具调用/数据访问留痕，脱敏后写入（#27）
- [ ] E2E 测试：挂起→审批→恢复全流程 + 拒绝 + 超时三条路径
