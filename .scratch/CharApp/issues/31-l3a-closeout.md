# 31 · L3a 收口（可观测：真机验收 + 文档同步 + 欠账清点）

**Status:** todo

**Type:** task

**Blocked by:** issue 27 · 28 · 29 · 30（四片全完）

**上游:** `CharApp/docs/PLAN.md` §2 的 L3a 行与 §5 的 L3a 段

## 这一片做什么

L3a 的四片是**并行可做**的（27→28 一条线，29 与 30 各自独立）。收口把它们收成一次**可验收的交付**，并把跑出来的欠账记清楚 —— **欠账不清点，下一片就会把它当成漏项重新发现一遍**。

## 一、真机验收（PLAN 定的两条，逐条走）

| # | 验收项 | 怎么走 |
|---|--------|--------|
| 1 | `trace` 列出**每一次工具调用**（工具名 / 参数 / 结果 / 耗时 / 状态） | 跑一轮带工具调用的真问答（比如问一次订单、再申请一次退款），拿 `run_id` 执行 `trace`，**把输出贴进本片** |
| 2 | 同一个入口报出**这次运行花了多少**（三档用量 + 折算金额） | 同上一次运行。金额要与供应商账单**对得上量级**（PLAN 的措辞是"量级"，不是"分毫不差"） |
| 3 | 日志脱敏**可被断言** | 构造一条含手机号的日志，落盘后搜不到原文；再搜一次搜索词（用一个像订单号的词），CharApp 侧任何日志里都没有 |

**另外两条要拿数字**（issue 30 的两条搭车）：

| # | 欠账 | 要记的数 |
|---|------|---------|
| 4 | BFF 共享 client | 改前 / 改后的首字节延迟（同一台机、同一个操作）。**如果收益不明显，如实写"不明显"并说明钱花在哪了** |
| 5 | 历史响应闸门 | 快速切三次会话，最终显示的是最后点的那一个 |

## 二、如实记一条"本阶段没有新增插件"

`PLAN.md` 早先提过 `CharAgent/plugins/observability`；**核实后那个包不需要建**：工具轨迹落记录层（控制状态的宿主）、成本分量已由 ADR-0005 落进 `runs` 五列（折算在查询侧）、日志脱敏是一个被调用的 `Redactor` 协议而不是挂载点。

**这一条要写进 PLAN 的 L3a 段**（已经写了，见 §5 的"核实后的修正"）**并在这里留一句结论** —— "本阶段没有新增插件"是**核实后的结论**，不是漏项。第一个框架侧的真实 hook 注册方出现在 L3b（业务注册的那条"需确认"裁决）。

## 三、文档同步

| 文档 | 改什么 |
|------|--------|
| `CharApp/docs/PLAN.md` §2 | L3a 行的「✅规划」→「✅」+ 完成日期 + issues 27–31 |
| `CharApp/docs/PLAN.md` §5 | L3a 段补真机结论（哪些兑现了、哪些改了口径） |
| `CharApp/CONTEXT.md` | 若本阶段定了新词就补；没有就**明确写"无新增词条"**（否则下一片会猜） |
| `CharAgent/docs/DESIGN.md` | #38 / #41 的"落地"栏补上 `trace` 入口与轨迹表 |
| `CharApp/docs/adr/` | 本阶段若跑出新的取舍就补 ADR |
| 根 `CLAUDE.md` / `README.md` | **不动** —— 已定「整个项目正式完成之后才同步」（`PLAN.md` §3.5） |

## 四、欠账清点（本片的主要产出之一）

把 27–30 四片**实际跑出来的**欠账逐条列出来，每条给：

- 它是什么（一句）
- 为什么当时没做（是取舍还是没发现）
- 建议归属（L3b / L4 / L5 / 单开一片 / 不做）

**已知会出现的候选**（实施时按实际补）：

- `trace` 的第二档（`--view` 展开帧）如果没做，它就在这里
- `Total_cost` 列保持 0 —— 这条**不是欠账**，是 ADR 口径（别列成待办）
- 框架 logger 命名不统一（5 个模块硬编码 `charagent.<子包>`，无集中常量）—— issue 29 明确不做，列出来**并注明"刻意不做"**
- `views_bff.py:471` 打原始会话编号的 `%r`（issue 29 的弱证据，没修）
- 其余实施中发现的

## 五、下一片（L3b）的开工前提

L3b 的五片（32–38）依赖 L3a 的 **27**（工具轨迹落库）。开工前逐条确认：

- [ ] `charagent_tool_calls` 有真实行，且 `status` / `arguments` / `result` / `duration_ms` 齐全
- [ ] `set_status` 的 UPDATE 路径验证过（L3b 要靠它写 `needs_approval` 与 `approved_by`）
- [ ] `messages` 的重复写入容错验证过（L3b 的挂起行引用 message_id）
- [ ] 上面第四节的欠账已归属完毕（没有"待定"）

## 验收

- [ ] 上面第一节的五条全部走完，**数字与输出贴进本片**
- [ ] 第二节那句结论写进 PLAN
- [ ] 第三节的文档同步完成（`git diff` 能看出改了哪几处）
- [ ] 第四节的欠账表**每条都有归属**，没有"待定"
- [ ] 第五节的四条开工前提逐条打勾

## 备注

- **收尾片不是"写总结"**：它的主要价值是**清点**与**交接**。issue 21 与 issue 15 都是这个形状，照它们写。
- **别在收口片里顺手做优化**：发现的问题记进第四节，不要当场改 —— 那会让"L3a 到底做完了没有"变模糊。
