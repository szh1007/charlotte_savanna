# 37 · 业务侧：下单前确认（框架级替代 prompt 层）+ prompt 改写

**Status:** todo

**Type:** task

**Blocked by:** issue 36（确认卡先能渲染）

**上游:** `CharApp/docs/PLAN.md` §5 的 L3b 段第 4 条；`../PRD.md` §4.7（「第三阶段：改为框架级 —— 工具执行前的挂载点返回"需要确认"，整个运行暂停并存档，用户确认后从存档点继续」）

## 一片一句话

把 L1 那句写在 prompt 里的"下单前问一句确认吗"**换成框架级的挂起**，并**把 prompt 里那句话删掉** —— 否则模型会既问一句又触发一张确认卡，用户被问两次。

## 这一片为什么排在最后

因为它是**纯复用**：`Decision.requires_approval`（34）、确认卡（36）、恢复端点（34/36）三样都在了，本片只加**一条裁决**、**删一句 prompt**。它是 L3b 的最后一个业务增量，也是"框架级确认能覆盖多个场景"的第二个证据。

## 一、护栏加一条裁决

在 `CharApp/minimall/guardrail.py`（L2 建的那个插件）里，对 `place_order` 返回：

```python
if tool.name == PLACE_ORDER_TOOL:          # 常量已在 :53
    return Decision.requires_approval(
        prompt="确认要下这一单吗？",
        needs=(),                          # 纯「是 / 否」，不要用户补任何数据
    )
```

**`needs=()` 是关键**：确认卡据此渲染成**只有两个按钮、没有输入框**（issue 36 定的两态）。代付那条是"带数据的确认"，这条是"纯是非的确认"—— **同一套机制，两种形态**，这正是本片要证明的东西。

## 二、两个插件同点共存：拒绝优先于挂起

`place_order` 现在会同时被**两条业务规则**看：护栏的"8 次写预算 / 5000 元上限"（L2）与刚加的"需要确认"（本片）。而 `HookRegistry.decide()`（`hooks/registry.py:180-217`）是**遍历 handlers 取第一个非放行者**（`:212-217`）。

于是**注册顺序会决定用户体验**：确认排在前面时，一个**会超预算**的下单会先弹确认卡、用户确认完才被拒。

**修法（在 issue 34 的 `decide()` 改动里一并做）**：**拒绝优先于挂起** —— 遍历全部 handlers，任一 `reject` 直接生效；没有任何 `reject` 时才看有没有 `requires_approval`。这样"拒绝"与"挂起"的语义各自干净，**不依赖注册顺序**。

**本片负责验它**：先用掉写预算（或下一个超 5000 的单），断言结果是一条**拒绝**而不是一张确认卡。

## 三、prompt 改写（这一片最容易被漏的一步）

L1 的 prompt 里有"下单前问一句确认吗"那类要求（`CharApp/minimall/prompt/system/v2.prompt`）。框架级挂起上线后，**那句话必须删**：

- 不删 → 模型先问一句，用户说"好"，模型再调工具 → **弹出第二张卡** → 用户被问两次
- 删了 → 模型直接调工具 → 挂起 → **卡片替模型问**

**做法**：出 `v3.prompt`（v2 不删，留着 —— 它是 L4 的 A/B 素材），把 `CharApp/minimall/prompt/manifest.yaml` 的 `default` 改成 `v3`。

> **另一种"必须改"的话，本片一并改掉**（issue 35 收尾时补记，2026-09-26）：
> v2 的第 1 条禁则写着「**不能替买家付款** …… 你没有、也不该有他的支付密码」。
> issue 35 把代付做出来之后，这句话**已经与事实相反** —— 模型读到它就会拒绝付款，
> 于是**代付这条链路在 v3 里仍然走不通**（35 的真机是拿一份临时提示词
> `D:/__WorkSpace__/Temp/hitl35prompt/system/v3.prompt` 跑通的，那一份就是这一节的
> 草稿：把「不能替买家付款」改成「**付款由买家本人点头** —— 你可以发起（调
> `pay_my_order`），但那一单会停下来等他本人输一次密码；不要向他要密码，也不要在
> 对话里再问一句"要付吗"」。**直接把那段搬进 v3 即可**。原来的票据只点了"删下单
> 确认那句"，没点这句 —— 是 35 收尾时发现的缺口，记在这里免得漏。）

**要记一笔的连带后果**：这会让 `runs.prompt_version` 从 `system/v2` 变成 `system/v3`。**L4 的评估与 A/B 必须以 v3 为基线** —— 别拿 v2 时期的跑分与 v3 之后的比。这件事写进 L4 的规划（`PLAN.md` §5 的 L4 段）。

## 交付物

| # | 内容 |
|---|------|
| 1 | `guardrail.py` 加 `place_order` 的 `requires_approval` 裁决 |
| 2 | （配合 issue 34）`decide()` 的"拒绝优先于挂起" |
| 3 | `prompt/system/v3.prompt`（删掉 prompt 层的下单确认要求，**并把付款那一节改成 issue 35 的新事实** —— 见 §三 的补记）+ `manifest.yaml` 的 `default: v3` |
| 4 | 用例：`place_order` 返回 `requires_approval` 而非 `allow`；**超预算时返回 `reject` 而非挂起**（两个插件同点的顺序） |
| 5 | `PLAN.md` §5 的 L4 段补一句"评估基线是 v3" |

## 验收

- [ ] 真机：说「帮我下单」→ **弹出确认卡且没有输入框** → 点确认 → 下单成功
- [ ] 点取消 → 模型收到一条工具结果（拒绝原因）并**告知用户没有下单**（不是静默失败）
- [ ] **模型没有在对话里额外问一句"确认吗"**（prompt 改写生效的正面证据）
- [ ] **说「帮我把这单付了」能走到确认卡**（付款那一节改写生效的证据 —— 拿 v2 跑会听到「我没有你的支付密码」）
- [ ] 一个会超 5000 元的下单：**直接拒绝**，不弹确认卡
- [ ] 写预算用完时的第 9 次写操作：**直接拒绝**，不弹确认卡
- [ ] `runs.prompt_version` 记的是 `system/v3`

## 备注

- **`prompt/system/v2.prompt` 不要删**：它是"prompt 层确认"与"框架级确认"对照的素材（L4 可以拿它跑 A/B 回答 PRD §4.7 那个问题："有多少比例被用户一句话绕过去了"）。
- **不做"记住这次选择，后面同类都别问"**：那是另一个功能（策略记忆），且它会削弱"每次都真的经过确认"这条演示价值。
- 本片做完，L3b 的业务增量就齐了 —— 直接进收口（issue 38）。
