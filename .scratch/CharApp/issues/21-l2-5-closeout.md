# 21 · 收尾：ADR · 文档 · 小尾巴 · 现场清理

**Status:** ready-for-agent

**Type:** task

**Blocked by:** 15–20（全部）

**上游:** `../PRD.md` §4.9（L2.5 行）、`CharApp/docs/PLAN.md` §2 / §5（L2.5）

## 做什么

L2.5 的收口片。**本片不新增功能** —— 只把决策记录、文档、遗留小尾巴与真机现场一次收干净。若发现 15–20 有遗留，回那边的 ticket 或新开一片，**不要在这里顺手补**（issue 14 立过的纪律）。

## 已经替你确认过的事实

| 事实 | 出处 |
|------|------|
| `CharApp/docs/adr/` 现有四条：0001 内部端点信任声明的身份 · 0002 浏览器→BFF 用 POST · 0003 工具事件业务侧脱敏 · 0004「退款中」做成订单状态取值 | `CharApp/docs/adr/` |
| **ADR 的先例是决策期就写**：0003 与 0004 都是在 L2 规划期（2026-09-21）落的盘，不是等实现完再补 | `CharApp/docs/PLAN.md` §6.3 的日期列 |
| ADR 的三条门槛（难回退 / 无上下文时反直觉 / 存在真实取舍）—— 0001 的记录里逐条写过 | `adr/0001-internal-endpoint-trusts-declared-user-id.md` |
| issue 13 留的一条尾巴：`session_for` 的 `redact` 想让它是**必填参数**（「第三个入口也得包」现在只靠 docstring 与 `redaction.py` 的模块说明提醒） | `issues/13` 的「代价说清」 |
| issue 14 留的一条：**「17」这个数字被复述 35 处 / 10 文件**（Shotgun Surgery），治法是全仓去数字化，「下次动工具集时一起做」 | `issues/14` 的两轴 review 表 |
| PLAN §3.5 记着：根 `CLAUDE.md` / `README.md` 的同步**已决意压后**到「整个项目正式完成」，不是漏项 | `CharApp/docs/PLAN.md:167-170` |
| `CharApp/minimall/__init__.py` 有一张结构总览表与一节「边界」，issue 12 / 14 各改过一次 | `CharApp/minimall/__init__.py` |
| `sh/charapp_backend.sh` 的头部注释列着前置条件（Django 在跑 · 根 `.env` 有令牌与模型 Key）—— **本批之后还要加 Postgres** | `sh/charapp_backend.sh:4-13` |
| L2.5 **不新增进程**（仍是 `sh/charapp_backend.sh` 那一个服务） | — |
| 真机现场（用户 2026-09-22 决定「先全留，闭环后清理」）：`l2accept_buyer`(id=26) · `l2accept_staff`(id=27) · 三张订单 · 退款单 id=5 · 两个验收期起的进程（Django :8000 / CharApp :1007） | 2026-09-22 验收记录 |

## 具体任务

### 1. 两条 ADR（照 0001–0004 的格式与长度）

**ADR-0005「一段对话有两种表示」**
- 决策：**账本是账本、视图是视图** —— 快照 append-only 存全量、只追加不改写；每次模型调用前把「系统提示 + 压缩摘要 + 最近 N 轮」算成一份**视图**发给模型；而**给人看的记录永不压缩**
- 三条门槛逐条写：**难回退**（它决定压缩写在哪、会话历史从哪读、前端看到什么，改它要动框架 + 业务 + 前端三处）· **反直觉**（"为什么用户看到的历史比模型看到的多"是必问题）· **真实取舍**（被否的三条：只压展示 / 只压发给模型的视图 / 两份都留全量 —— 各自的代价写清）
- 代价照记：**两边的历史从此不一致**，排查"模型为什么忘了"要去快照、排查"用户看到什么"要去记录表

**ADR-0006「对话记录落在框架自己的 db 层」**
- 决策：`charagent_threads` / `charagent_runs` / `charagent_messages` 三张表由框架写入与查询，`tenant_id` / `user_id` 进 `RunContext`
- 被否的替代：业务侧 Django/MySQL 新建模型（框架已经把这套设计完了，业务侧重建等于抄一遍 `hidden` / `reasoning` / `tool_call_ids`）· 只靠快照（用错表：快照是循环的工作数据，Redis 模式的 `MAXLEN` / TTL 会吃掉历史）· 让业务自己加路由（框架的 `db/` 包就永远没有生产调用方 —— 而"没人用过的接口等于没设计过"正是这个项目立项的理由）
- 代价照记：**Postgres 成为业务的硬依赖** · **框架从此要认识两个身份字符串**（`payload` 不透明那条纪律的有意例外）· 记录是**事后投影**，进程被 kill 在写入前会丢最后一轮

### 2. 文档核对（大部分已在规划期改好）

| 文件 | 状态 |
|------|------|
| `CharApp/CONTEXT.md` | **规划期已改**：新增「会话运行时」一节（会话 / 运行 / 快照 / 记录 / 上下文视图 / 压缩摘要）+ 改写「取消」「退款」「退款申请」三条 —— 本片只**核对** |
| `../PRD.md` | **规划期已改**：§4.5 两条改判 + 补充说明 + §4.9 新增 L2.5 行 + L4 计数修正 + 五条修订记录 —— 本片只**核对** |
| `CharApp/docs/PLAN.md` | **规划期已改**：§2 加 L2.5 行与 L2 的 ✅、§5 加 L2.5 概要 —— 本片只**核对** |
| `CharApp/minimall/__init__.py` | **本片要改**：结构总览表补 L2.5 新增的模块（记录装配 / 上下文参数的落点），「边界」一节改写成 L2.5 之后的状态 |
| `sh/charapp_backend.sh` | **本片要改**：前置条件补「Postgres 在跑 + `alembic upgrade head` 已跑过」 |
| 根 `CLAUDE.md` / `README.md` | **不动**（PLAN §3.5 的决议：整个项目正式完成后才同步）—— 本片确认 `git status` 对这两份干净 |

### 3. 遗留小尾巴

- **issue 13 的 `redact` 必填参数**：已在 issue 18 里安排（正好两个入口都要改）。**本片核对**：若 18 没做，在这里补上
- **issue 14 的「17」去数字化（35 处 / 10 文件）**：**继续押后**。理由：本批**没有**动工具集或计数语义，现在做等于为一个静态数字付一次全仓改写；而 L4 的「工具数量 A/B」必然要动工具集，那时候一起做，收益与成本才对齐。**把这条决定与理由写进 PLAN §5 的 L4 行旁边**，免得下一轮又被当漏项提出来（issue 14 的教训：写在 ticket 里的待办如果没人认领就等于不存在 —— 这次给它一个明确的认领时机）

### 4. 现场清理

- 删掉 `l2accept_buyer`(id=26) / `l2accept_staff`(id=27) 两个账号 —— 注意 `Order.user` 是 `PROTECT`，要**先删订单**（三张：两张 cancelled、一张 refunded）与退款单 id=5
- 关掉验收期起的两个进程（Django :8000 / CharApp :1007），**确认清理后服务仍能正常启动**（起一次、问一句、停掉）
- 清理完把结果（删了什么、剩什么）写进本 ticket

### 5. 质量位

- 三套测试全绿：框架（`pytest CharAgent`，含 `-m pg` / `-m pg_db` 两个默认排除的标记）· 业务（`pytest CharApp`）· 商城（`manage.py test app.minimall`，**耗时较长，由用户手动跑**）
- `ruff check` / `ruff format --check` 干净
- 根 `CLAUDE.md` / `README.md` 的 `git diff` 为空

## 验收

- [ ] ADR-0005 / 0006 落盘，格式与长度对齐 0001–0004；每条都写明**被否的替代方案**与**代价**
- [ ] `CharApp/CONTEXT.md` 核对无误（五个新词条 + 三条改写都在）
- [ ] PRD 与 PLAN 的规划期改动核对无误；PRD 修订记录里能找到 L2.5 与规则改判两条
- [ ] `CharApp/minimall/__init__.py` 的总览表与「边界」反映 L2.5 之后的真实状态
- [ ] `sh/charapp_backend.sh` 的前置条件补上 Postgres 与 `alembic upgrade head`
- [ ] issue 18 的 `redact` 必填参数确认已做（没做则本片补上，并回写 18）
- [ ] 「17」去数字化**仍然押后**，且 PLAN §5 的 L4 行旁写明了"下次动工具集时一起做"
- [ ] 真机现场清理完成：两个账号 / 三张订单 / 退款单 / 两个进程；清理后服务能起、能问一句
- [ ] 三套测试全绿（商城那套用户手动跑）· `ruff` 干净
- [ ] 根 `CLAUDE.md` / `README.md` 未被改动（`git status` 干净）

## 备注

- **本片不新增功能**，只收口。发现 15–20 的遗留 → 回那边的 ticket 或新开一片
- **为什么 ADR 现在写而不是实现完再补**：与本项目 0003 / 0004 的先例一致 —— 决策一旦拍板，理由与代价最清楚的时候就是**现在**；等实现完再写，会不自觉地把结果当成理由
- **L2.5 到此收口**。下一片是 L3（可观测 / 成本记账 / HITL / 助手代付），立项前先把根文档同步那次独立动作排期（挂在「整个项目正式完成」之后）
