# 14-P1-4 — 幂等 + Saga 补偿 + 分布式锁

**What to build:** 幂等：真实动作（退款/通知）挂幂等键，同 request_id 重复提交直接返回已有结果、副作用不重复执行（#17）；Saga 补偿：正向执行链 + 失败逆序补偿，保证真实副作用可回滚；分布式锁：Redis SETNX + TTL 续租 + owner 校验，防惊群（#21）。

**Blocked by:** 11

**Status:** ready-for-agent

- [ ] 幂等键全链路：POST 接受 request_id → 重复提交返回已有结果、副作用不重复（#17）
- [ ] Saga：多步副作用流程 + 失败逆序补偿（退款/通知场景）（#17）
- [ ] 分布式锁：SETNX + TTL 续租 + owner 校验（防误删他人锁）（#21）
- [ ] 锁边界测试：竞争/超时续租/释放误删防护
- [ ] 测试：同 request_id 重复提交副作用只发生一次（#13/#17）
- [ ] **Saga 原语**：用假三步流程测试（**不写业务链路**）—— 任一步失败逆序补偿，补偿动作本身幂等

---

**2026-09-18 修订（两轮）**：本 issue **只交付通用原语**，不绑定任何业务链路。

- 第一轮曾把「异步退款 saga（改余额 + 写流水 + 置状态）」写成本 issue 的验收项 —— 那是**业务链路**，已移出。真正的链路归 `CharService/issues/06`（worker 在 **minimall** 侧，同事务改余额 + 写流水 + 置状态）
- 框架侧的正确表述：交付 **Saga 补偿原语**（正向链 + 逆序补偿 + 补偿幂等），用假三步流程测试；真实副作用场景由应用在自己的链路里消费该原语
- 幂等键同理：框架交付 `IdempotencyStore` 的 Redis/PG 实现与 TTL；**哪些动作需要幂等键由应用声明**
