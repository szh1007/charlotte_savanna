# 13-P1-3 — 熔断 + failover + 分层超时 + 工具取消

**What to build:** 熔断器（关闭/半开/打开三态，供应商连续失败时打开，自动 failover 到备份模型）；分层超时（model 60s / tool 10-30s / run 总超时）；卡死的工具可被取消（工具协程取消 + 资源释放）。熔断状态与降级路径（模板回复 + 转人工建议）衔接。

**Blocked by:** 11

**Status:** ready-for-agent

- [ ] 熔断三态转换正确（连续失败 → 打开 → 半开试探 → 恢复/保持）（#14）
- [ ] failover：主模型熔断后自动切备份模型（#14）
- [ ] 分层超时：model 60s / tool 10-30s / run 总超时三档独立生效（#15）
- [ ] 工具取消：超时后工具协程取消、连接释放（#15）
- [ ] 测试：熔断状态机边界、超时触发点、failover 路径（#14/#15）
- [ ] **通用下游依赖熔断**：熔断器可作用于**任意**被配置的下游依赖（不止模型），与模型熔断共用同一套三态实现；故障时上报通用码 `DEPENDENCY_DOWN`

---

**2026-09-18 修订（两轮）**：新增一条熔断维度，但**表述按通用口径**。

- 熔断器本身是通用机制：三态 + 阈值 + 半开探测，**作用于被配置的下游依赖**，不为某个业务系统特化
- 第一轮曾写成「下游业务系统（网关 / minimall internal）故障」并规定「只读仍可用、写操作拒绝并建议转人工」—— 后半句是**产品策略**，已移出（归 `CharService/issues/09` 的降级出口）；错误码也从 `GATEWAY_DOWN` 改为通用的 **`DEPENDENCY_DOWN`**（框架不该知道「网关」这个业务拓扑）
